<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jarchess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_king_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_king_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".match.gui.MatchActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".match.gui.EasyAIMatchActivity" />
        <activity android:name=".match.gui.HardAIMatchActivity" />
        <activity android:name=".match.gui.LocalMultiplayerMatchActivity" />
    </application>

</manifest>